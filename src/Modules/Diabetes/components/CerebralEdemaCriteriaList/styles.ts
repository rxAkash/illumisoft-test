import { StyleSheet } from "react-native";
import { blackColor } from "UIToolkit/theme/colors";

const styles = StyleSheet.create({
  card: {
    padding: 12
  },
  sectionTitle: {
    fontSize: 16,
    color: blackColor,
    marginBottom: 14,
    fontWeight: '500'
  },
  diagnosisCriteriaTitle: {
    fontSize: 15,
    color: blackColor,
    fontWeight: '500',
    marginBottom: 4
  },
})

export default styles;
