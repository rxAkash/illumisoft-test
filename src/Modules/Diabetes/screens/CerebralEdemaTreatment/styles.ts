import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  warningText: {
    color: 'red',
    fontWeight: '600',
    fontSize: 14
  },
  bold: {
    fontWeight: '600'
  }
})

export default styles;
