import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  loadingContainer: {
    position: 'absolute',
    top: 50,
    left: 0,
    right: 0,
    bottom: 0
  }
})

export default styles;
